/**
 * Загрузка фото для старой формы /admin-add.
 *
 * Здесь был собственный разбор multipart: файл писался на диск вручную
 * (а потом тот же файл писал Payload — в хранилище оставался мусор с именем
 * из браузера, включая «../» в пути), авторизация не проверялась вовсе —
 * загрузить файл на сервер мог любой, кто знает адрес. Теперь это тот же
 * обработчик, что и в CRM (/api/crm/upload): проверка входа, формата и
 * размера, запись файла силами Payload. Ответ прежний — { doc }.
 */
export { POST } from '../crm/upload/route'
