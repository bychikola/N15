# Дизайн: Перенос главной-лендинга из прототипа N15-chatgpt

Дата: 2026-08-15 · Статус: утверждён пользователем (все секции одобрены)
⚠️ Работа без коммитов в git до явной команды пользователя.

## Контекст

Прототип `C:\Users\Admin\Documents\N15-chatgpt` — одностраничный лендинг (vinext/vite, запускается локально на :3100 с мок-БД). Текущий сайт N15 — многостраничный (Next 16 + Payload).

**Решения пользователя:**
- Одностраничник: все секции прототипа на главной
- Палитра прототипа (#FAF8F3 светлая тёплая), шрифт — New Standard (текущий, вместо Baskerville)
- Фильтры «Что вы ищете?» → каталог с выбранными параметрами; в каталог добавляется фильтр «Район»
- Объекты — из Payload; тексты секций — в i18n (ru+os)
- Подход A: лендинг изолирован scoped-стилями, глобальные токены сайта не трогаем

## Секции лендинга (сверху вниз)

1. **Hero** (680px): фоновое фото `/img/hero.jpg` (из прототипа, в public) + тёмный оверлей; этикетка «Владикавказ · Северная Осетия — Алания»; H1 «Недвижимость в Осетии» (New Standard ~72px, белый); подпись; кнопка «Выбрать объект →» (якорь #objects); примечание «Осетия — наш главный рынок»
2. **«Что вы ищете?»** (#objects): заголовок + 4 аккордеона-категории (01 Квартиры / 02 Частные дома / 03 Земельные участки / 04 Коммерческая). Чипы → ссылки в каталог:
   - комнатность: `?category=apartment&rooms=1|2|3|4`
   - районы: `?category=…&district=<Район>`
   - пригород/населённые пункты/СНТ — текстовые справочники (как в прототипе), ссылки только на категорию без района
3. **Избранные объекты**: Payload Objects (published, 6 последних по createdAt), карточки с фото-подложкой (primaryImage), бейдж категории, адрес+район, заголовок, строка характеристик, hover «Открыть объект →»; ссылка `/catalog/[id]`. Плейсхолдеры при пустой выдаче
4. **Ипотечный калькулятор**: рабочая логика из прототипа (аннуитет): стоимость, взнос ₽/%, срок, ставка → ежемесячный платёж. Ссылка «Ипотечный брокер» → юр-блок
5. **Загородная недвижимость**: «Ближний пригород Владикавказа» + 9 аккордеонов районов с населёнными пунктами (данные — i18n-справочник, копия из прототипа). Текст, не фильтры
6. **Дизайн и ремонт**: 2 карточки услуг (тексты i18n)
7. **Юридические услуги**: аккордеон «Экспертный центр N15» → 6 карточек + кнопка «Получить консультацию →» (якорь #contact)
8. **О компании**: фото Фатимы `/img/fatima-ossetia.png`, «Осетия — наш приоритет», сигнатура «Фатима, дарящая солнце», аккордеон «Другие направления»
9. **Контакты** (#contact): «Найдём объект в Осетии», телефон из SiteSettings (не хардкод), кнопка «Позвонить»

## Каталог: фильтр «Район»

- `CatalogFilters`: новый dropdown «РАЙОН» (Любой + Иристонский, Северо-Западный, Затеречный, Промышленный)
- `buildWhere`: `'address.district': { equals }`; URL-параметр `district`; читается/пишется как остальные фильтры (URL-sync уже реализован)
- i18n: список районов

## Стиль лендинга

- Scoped CSS в `src/app/globals.css` (секция «Landing»): фон `#FAF8F3`, текст `#1D1A16`, muted `#6B655B`, акцент-рамки; карточки/чипы — pill-стиль прототипа
- Заголовки New Standard; капс-этикетки с трекингом
- Тёмная тема лендинг не затрагивает (секции светлые всегда — как в прототипе)

## Технические детали

- Главная — server component; Payload-запросы на сервере (объекты, SiteSettings)
- Новые компоненты `src/components/home/`: `LandingHero`, `SearchCategories`, `FeaturedObjects`, `MortgageCalculator`, `CountryGuide`, `ServicesSection`, `LegalSection`, `AboutSection`, `ContactSection`
- Калькулятор — client component (интерактив)
- Ассеты из прототипа: `public/img/hero.jpg`, `public/img/fatima-ossetia.png` (+ плейсхолдеры квартиры/виллы при наличии)
- i18n: секции + справочник районов/населённых пунктов (ru+os)
- **Без git-коммитов до команды пользователя**

## Тестирование

- tsc + lint
- Браузер: все секции рендерятся, аккордеоны открываются, чипы ведут на каталог с правильными параметрами, калькулятор считает, объекты из БД кликабельны
- Проверка: фильтр района в каталоге (выбор в dropdown → URL → отфильтровано; ссылка с лендинга → фильтр применён)
