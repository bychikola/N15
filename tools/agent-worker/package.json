{
  "name": "n15-agent-worker",
  "version": "1.1.0",
  "private": true,
  "description": "Воркер ИИ-агента для CRM N15: берёт задачи из очереди (agent-tasks) и выполняет через настоящий Claude Code CLI (headless), затем проверяет и деплоит.",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "imapflow": "^1.7.8",
    "mailparser": "^3.9.20"
  }
}
