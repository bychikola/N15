{
  "name": "n15-agent-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Воркер ИИ-агента для CRM N15: берёт задачи из очереди (agent-tasks), выполняет через Claude Agent SDK и деплоит.",
  "main": "worker.js",
  "scripts": {
    "start": "node worker.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.12",
    "pg": "^8.13.0"
  }
}
